{
  "outputDirectory": ".next",
  "buildCommand": "npm run build",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "https://madavpn-api.azurewebsites.net/api/:path*"
    }
  ]
}
